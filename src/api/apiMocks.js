import categories from "../mocks/categories";
import items from "../mocks/items";

export const callApiCategories = () => {
    return categories;
};

export const callApiItems = () => {
    return items;
};
